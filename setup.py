from setuptools import setup, find_packages

setup(
    name='lilytorch',
    version='0.1',
    packages=find_packages(),
    install_requires=[
    ],
    # Metadata
    author='',
    author_email='',
    description='',
    url=''
)